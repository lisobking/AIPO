# 📝 공수산정서→견적서 초안 변환 파이프라인 구현

- **일시:** 2026-05-19
- **담당 에이전트:** 클코 (개발자 1), 디자이너, PM (박부장)
- **작업 유형:** `feat`
- **Git Commit:** `1cac269`

## 🎯 목표 (Goal)
- 공수산정내역서(Excel)를 입력받아, 항목 + M/D 수량 추출 후 사용자 지정 단가를 적용해 견적서 초안(Excel)을 자동 생성.
- 비용 계산: 옵션 A (단가 고정값 입력 → 자동 계산) 방식 채택.

## 🛠 수정/생성된 파일 (Touched Files)
1. **`app_main.py`**
   - `find_effort_header()`: 공수산정서 헤더 행 자동 탐지 (순번/항목 키워드 기반).
   - `find_effort_col()`: 공수(M/D) 컬럼 자동 탐지.
   - `find_item_col()`: 항목명 컬럼 자동 탐지.
   - `process_effort_to_quote()`: 핵심 변환 로직 (파싱→계산→Excel 출력).
   - `/upload_quote` API 라우트 신설.
   - 신규 경로: `workspace/03_input_effort/`, `workspace/04_output_quotes/`.
2. **`web/index.html`**
   - 듀얼모드 토글 UI (발주서 변환 / 견적서 생성).
   - 다크 글라스모피즘 테마 적용.
   - M/D 단가 입력 필드 추가 (기본값 ₩1,350,000).

## ✅ 검증 결과 (Verification)
- NH투자증권 공수산정서 샘플 테스트 → 16건 항목 추출 성공, 합계 ₩256,500,000 정상 계산.

## ⏭️ 다음 작업자를 위한 인수인계 (Handover)
- 유한양행 샘플(Sheet2 구조) 다중 시트 대응 필요 여부 확인.
- 견적서 템플릿 서식 주입 방식(기존 .xls 템플릿 활용) 후속 논의 필요.
