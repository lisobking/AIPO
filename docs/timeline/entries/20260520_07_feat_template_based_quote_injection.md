# 📝 공식 기업용 템플릿 기반 견적서 초안 생성 파이프라인 구축 및 검증 완료

- **일시:** 2026-05-20
- **담당 에이전트:** PM (박부장), 개발자 1 (클코), 개발자 2 (Opus), QC
- **작업 유형:** `feat`
- **Git Commit:** `N/A`

## 🎯 목표 (Goal)
- 사용자가 제공한 실제 가온아이 정식 엑셀 견적서 양식(`유한양행 신규 양식 2종 포함...xlsx`)의 빨간 테두리(세부 내역 및 헤더/푸터 영역) 구조를 100% 원형 보존하면서, 우리가 공수산정서로부터 파싱해 낸 세부 품목, 수량(M/D), 단가, 금액을 동적으로 자동 주입하는 하이브리드 파이프라인 개발 및 검증.

## 👥 에이전트별 업무 분담 (Multi-Agent Task Assignment)
1. **PM (박부장)**:
   - 가온아이 공식 표준 견적서 양식을 파이프라인의 고정 템플릿(`Standard_Quote_Template.xlsx`)으로 채택하여 기업용 문서 정체성을 철저히 지키도록 지휘.
2. **개발자 1 (클코 - 코어 백엔드)**:
   - `app_main.py` 내 `process_effort_to_quote` 변환 모듈을 전면 개편.
   - `openpyxl`을 활용하여 병합 영역(Gu-bun, Unit Price, Proposal Total)의 특수 제약 조건(`AttributeError: 'MergedCell' object attribute 'value' is read-only`)을 우회하는 정밀 unmerge → data write → dynamic re-merge 알고리즘 완벽 구현.
   - 한글 금액 환산 함수 `num_to_kor` 설계 및 주입 완료 (예: 13,500,000원 → `일천삼백오십만`).
   - 단가 입력 마스킹 연동 및 `F*G14` 형태의 다이나믹 엑셀 연산 수식 자동 주입 설계.
3. **QC**:
   - 로컬 테스트 환경을 구축하고 `scratch/test_conversion.py` 스크립트를 가동하여 실제 유한양행 공수산정서 엑셀 파일을 변환 및 셀 정합성 스트레스 테스트 통과 완료 (Exit Code: 0).

## 🛠️ 수정/생성된 파일 (Touched Files)
1. **`app_main.py`**
   - 가온아이 표준 견적서 동적 주입 및 레이아웃 재병합 모듈 리팩토링.
2. **`template/Standard_Quote_Template.xlsx`** (신규 복사)
   - 가온아이 공식 기업용 엑셀 견적서 템플릿 등록 완료.

## ✅ 결과 검증 (Verification)
- 유한양행 샘플 변환 결과:
  - `D4` (수신처): `유한양행` 동적 주입 확인.
  - `B9` (제안서 서두 문구): `유한양행 "신규양식 2종 포함 총 양식 6종 및 SSO 모듈 추가 기술지원"건으로...` 정확히 동적 치환 확인.
  - `B10` (제안금액): `제안금액 :일금일천삼백오십만원정 (₩13,500,000) V.A.T 별도` 변환 및 포맷팅 확인.
  - `B/G/I` 세로 병합 범위가 파싱 품목 개수(3건)에 맞추어 `13~16`행까지 동적으로 자동 병합 영역 연동 확인.
