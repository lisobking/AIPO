# 📝 파이프라인 고도화: 대괄호 고객사명 파싱 + 빈 행 초기화 + MergedCell 방어

- **일시:** 2026-05-20
- **담당 에이전트:** PM (박부장)
- **작업 유형:** `feat`
- **Git Commit:** `N/A`

## 🎯 목표 (Goal)
- er2 폴더에 보관된 한양대의료원 완성 견적서의 문제점 분석 및 파이프라인 엔진 보강.
- 공수 없는 템플릿 기본행(* 개발, * 테스트 등)이 초기화되지 않고 잔존하는 문제 해결.
- 대괄호가 포함된 파일명(예: `[한양대의료원]`)에서 고객사명이 깨지는 문제 해결.

## 🛠️ 조치 사항 (Actions Taken)
### 수정 파일: `app_main.py`

1. **파일명 파서 개선 (Line 191~197)**:
   - 대괄호 `[]`를 공백으로 치환하여 고객사명 토큰 정확 분리 (`[한양대의료원]SSO` → `한양대의료원`, `SSO`)
   - `YYYYMMDD` 8자리 날짜 제거 정규식 추가 (`20260520` 잔존 방지)
   - `YYYY.MM.DD` 정규식에서 `\b` 워드 바운더리 제거로 매칭 성공률 개선
   - 6~7자리 숫자코드(견적번호 등) 제거 추가

2. **빈 행 초기화 보강 (Line 322~330)**:
   - 기존: C열, D열, F열, H열만 초기화
   - 변경: C~I열(3~9열) 전체 초기화로 기능별 라벨(관리/설계/개발/이행) 잔재 완전 제거
   - `MergedCell` 방어: `isinstance(cell, MergedCell)` 체크 추가로 병합 셀 쓰기 AttributeError 방지

## ✅ 결과 검증 (Verification)
- 한양대의료원: D4=`한양대의료원` ✅, Row 16~17 완전 초기화 ✅, Sheet2 원본 복제 ✅
- NH투자증권: D4=`NH투자증권` ✅, 16건 품목 정상 주입 ✅
- 유한양행: D4=`유한양행` ✅, 3건 품목 + 날짜 제거 ✅

## ⏭️ 다음 작업자를 위한 인수인계 (Handover)
- 3사 교차 검증 100% 통과 완료.
- Render 서버 반영 필요 (git push → auto deploy).
