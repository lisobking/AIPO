# 📝 고객사명 NFD 자모 분리 해결 및 원본 공수산정 시트 무가공 복제 복합 파이프라인 구축 완료

- **일시:** 2026-05-20
- **담당 에이전트:** PM (박부장), 개발자 1 (클코), QC
- **작업 유형:** `feat`
- **Git Commit:** `N/A`

## 🎯 목표 (Goal)
- **한글 자모 분리 현상 정복**: macOS 특유의 NFD 조합형 인코딩으로 인해 발생하는 한글 자모 불리 현상(`ㅇㅠㅎㅏㄴㅇㅑㅇㅎㅐㅇ`)을 해결하여 깔끔한 완성형 한글(`유한양행`)로 치환.
- **오리지널 레이아웃 보존 (표 깨짐 방지)**: 공수 데이터 개수가 기본 템플릿 기본 행(4개)보다 적더라도, 기존 템플릿의 표준 `이행` 단계 텍스트(`* 테스트 및 운영서버 반영`)를 강제 삭제하지 않고 보존하여 템플릿 정합성 유지.
- **원본 무가공 백업 시트 복제**: 업로드된 오리지널 공수산정서 Excel 시트 전체를 우리가 생성하는 견적초안의 두 번째 시트(`공수산정근거`)에 스타일/너비/높이/병합/값 등 어떠한 왜곡도 없이 그대로 추가 기입 복사.

## 👥 에이전트별 R&R 수행
1. **PM (박부장)**:
   - 두 번째 시트에 무가공 원본 공수산정서를 복제하여 견적서 1페이지와 산정 근거를 단일 파일로 열람할 수 있도록 하이브리드 비즈니스 문서 설계안 수립.
2. **개발자 1 (클코)**:
   - `app_main.py` 내에 `unicodedata.normalize('NFC', ...)`를 전면 주입하여 자모 분리 완벽 차단.
   - `if num_items < default_rows:` 루프에서 C, D열의 텍스트 보존 루프 적용.
   - openpyxl Workbook 객체를 다이나믹하게 순회하며 원본 워크북의 열 너비(`column_dimensions`), 행 높이(`row_dimensions`), 병합 영역(`merged_cells`), 폰트 및 테두리/정렬 등 셀 스타일을 `공수산정근거` 시트로 복제하는 딥카피(Deep-copy) 병합 모듈 구축.
3. **QC**:
   - `scratch/test_conversion.py` 를 재가동하여 변환 결과 수신처가 완성형 `유한양행`으로 깨짐 없이 들어가고, 두 번째 백업 시트가 원형 복제되었음을 검증 통과 완료.

## 🛠️ 수정된 파일 (Touched Files)
1. **`app_main.py`**
   - NFC 한글 정화 장치 탑재, 표준 공정 보존, 원본 무가공 시트 2 복제 모듈 리팩토링 및 저장.

## ✅ 결과 검증 (Verification)
- 유한양행 샘플 E2E 변환 최종 결과:
  - `D4` (수신처): `유한양행` (NFC 정규화 완성형 복구 완료)
  - `B9` (서두): `유한양행 "신규양식 2종 포함 총 양식 6종 및 SSO 모듈 추가 기술지원"건으로...` (자모 분리 흔적 영구 차단)
  - `Row 17` (이행 단계): `이행` 및 `* 테스트 및 운영서버 반영` 텍스트 원형 보존 확인.
  - `Sheet 2` (공수산정근거): 업로드한 유한양행 원본 공수산정 엑셀 시트 전체가 포맷 왜곡 없이 그대로 적재됨을 확인.
